# Multiple Simulation Envelope App

A specialized UI pipeline scaling the core population ethology
topologies directly into extensive sequence horizons producing bounds
and dynamic envelope metrics.

## Usage

``` r
multApp(title = "Population Ethology")

multServer(id)

multInput(id)

multOutput(id)
```

## Arguments

- title:

  Application title

- id:

  module ID string
