# Build Interactive Base Map

Constructs a blank interactive Leaflet mapping canvas optimized for
North America geographic search. It embeds the
\`leaflet.extras::addSearchOSM()\` search bar, allowing text-based
flying capabilities (Option A interaction paradigm).

## Usage

``` r
build_base_map()
```

## Value

A \`leaflet\` HTML widget object
