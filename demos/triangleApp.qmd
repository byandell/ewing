---
title: "triangleApp (Shinylive)"
description: "A spatial geometric network visualizer rendering triangular substrates, hexagonal cell overlays, and connectivity topologies."
author: "Brian S. Yandell"
engine: knitr
toc: false
image: "images/triangle.png"
format:
  html:
    embed-resources: false
---

[← Back to Demos Gallery](index.qmd)

The live application below is running completely client-side in your browser using serverless **Shinylive** (WebAssembly). You can interactively adjust substrate width, step density, toggle plant modules, and inspect spatial grid statistics.

```{r, echo=FALSE, results='asis'}
inc_files <- c("R/triangle.R", "R/substrate_triangle.R", "R/triangleApp.R")

cat("```{shinylive-r}\n")
cat("#| standalone: true\n")
cat("#| viewerHeight: 750\n")
cat("#| components: [viewer]\n\n")

cat("library(shiny)\n")
cat("library(bslib)\n")
cat("library(ggplot2)\n")
cat("library(stats)\n\n")

for (f in inc_files) {
  src <- readLines(file.path("..", f), warn = FALSE)
  cat(paste0("# --- Auto-Included from ", f, " ---\n\n"))
  cat(paste(src, collapse = "\n"))
  cat("\n\n")
}

cat("# --- Launch Application ---\n\n")
cat("app_theme <- bslib::bs_theme(version = 5, bg = '#ffffff', fg = '#212529', primary = '#2a9d8f')\n")
cat("ui <- bslib::page_sidebar(title = 'Triangular Substrate Explorer (triangleApp)', theme = app_theme, sidebar = bslib::sidebar(width = 320, triangleAppInput('tri')), triangleAppOutput('tri'))\n")
cat("server <- function(input, output, session) { triangleAppServer('tri') }\n")
cat("shiny::shinyApp(ui = ui, server = server)\n")
cat("```\n")
```

---

## Programmatic Application Usage

Launch the interactive application natively in R:

```r
library(ewing)

triangleApp()
```
